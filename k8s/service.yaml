apiVersion: v1
kind: Service
metadata:
  name: tdl-api-service
spec:
  type: NodePort
  selector:
    app: tdl-api
    version: blue
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 31830